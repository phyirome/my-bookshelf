<%= simple_form_for @book do |f| %>
  <%#= select_tag(:category_id, options_for_select(@categories), :prompt => "Select a category") %>
  <%= f.file_field :image, id:"book-img" %>
  <%= f.input :title, label: "Book Title" %>
  <%= f.input :author %>
  <%= f.input :publisher %>
  <%= f.input :description %>
  <%= f.button :submit, value="完了" %>
<% end %>